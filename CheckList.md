# To do 

- [...] Simulate with 6 cot : 
    - [x] Write the code     
    - [x jaguar] config_cot6_soap_sft.json            (~ base) --> poor
    - [x SJTU] config_cot6_adam_sft.json          (~ base) --> poor
    - [x mazda] config_cot6_soap_sft_overspl.json     (+ oversampling) --> Ok 81%
    - [x nissan] config_cot6_soap_dft_overspl.json    (+ oversampling + DFT) --> DFT does not perform well on this example
    - [x rover] config_cot6_soap_dwl_dft_overspl.json (+ oversampling + DFT + DWL) --> not good maybe due to DFT
    - [porsche & x SJTU] config_cot6_soap_dwl_sft_overspl.json (+ oversampling + SFT + DWL) -> poor results
    - [x royce] config_cot6_soap_dwl_dft_overspl_v2.json (+ oversampling + DFT + DWL) (other weights scheduling) --> not good maybe due to DFT
    - [x SJTU] config_cot6_soap_dwl_dft_overspl_optimizedscheduling.json (+ oversampling + DFT + DWL) (optimized weights scheduling) --> not good maybe due to DFT
    - [jaguar] config_cot6_soap_dwl_sft_overspl_optimizedscheduling.json (+ oversampling + SFT + DWL) (optimized weights scheduling)
    - [x SJTU] config_cot6_soap_dwl_sft_overspl_optimizedscheduling.json (+ oversampling + SFT + DWL) (optimized weights scheduling) -> Ok 82%
    - [SJTU] config_cot6_soap_dwl_sft_overspl_veryconstrastedscheduling.json (+ oversampling + SFT + DWL) (optimized weights scheduling)
- [ ] Do DPO on the best performing one : 
    - [~] Write the code
    - [ ] Generate the DPO dasaset from the best finetuned model
    - [ ] DPO without finetuning 
    - [ ] DPO without finetuning with oversampling
    - [ ] DPO after the best finetuning
    - [ ] DPO after the best finetuning with oversampling
- [ ] Finetuning a BERT model 
- [ ] Extend the benchmarking to other french benchmarks to compare.
- [ ] Look closer at the Llamaguard configuration to ensure we used the best possible settings.
- [ ] Simulate on direct classification